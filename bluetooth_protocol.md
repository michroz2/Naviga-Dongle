# Спецификация BLE Протокола Naviga 
**Версия:** 1.29  
**Устройства:** Naviga-Dongle (C++) ⟷ Naviga-Operator (Flutter/Dart)

## 1. Базовые правила (Общие для всех пакетов)
* **Порядок байтов (Endianness):** Little Endian.
* **Выравнивание памяти (Padding):** Строго 1 байт (отсутствует). В C++ используется `#pragma pack(push, 1)`. Приложение должно читать байты строго подряд без пропусков.
* **Строки (char[]):** Фиксированный размер массива. Строка завершается нуль-терминатором (`\0`, `0x00`). При парсинге необходимо обрезать строку по первому встречному нулевому байту.
* **Размер MTU:** Максимальный размер пакета составляет 39 байт. Рекомендуется запросить MTU не менее 64 байт при подключении.

---

## 2. Профиль GATT (Идентификаторы BLE)
* **Имя устройства (Device Name):** `Naviga-Dongle`
* **Service UUID:** `6E400001-B5A3-F393-E0A9-E50E24DCCA9E`
* **RX Characteristic UUID:** `6E400002-B5A3-F393-E0A9-E50E24DCCA9E`
  * *Направление:* Смартфон ➔ Донгл
  * *Свойства:* WRITE, WRITE_NO_RESPONSE
* **TX Characteristic UUID:** `6E400003-B5A3-F393-E0A9-E50E24DCCA9E`
  * *Направление:* Донгл ➔ Смартфон
  * *Свойства:* NOTIFY

---

## 3. Таблица Команд (OpCodes)

Первый байт ЛЮБОГО пакета всегда является кодом операции (`OpCode`).

### Команды в RX (Смартфон ➔ Донгл)
| OpCode | Константа | Описание | Payload (Ожидаемая структура) |
|---|---|---|---|
| `0x01` | `CMD_SET_IDENTITY` | Задать Имя и Роль Донгла | Структура `BleIdentity` |
| `0x02` | `CMD_SET_SYS_CONFIG` | Задать таймеры системы | Структура `BleSysConfig` |
| `0x03` | `CMD_ACTION_RESET` | Аппаратная перезагрузка (Hard Reset) | Нет (только 1 байт: `[0x03]`) |
| `0x04` | `CMD_ACTION_CLEAR_DB`| Очистить базу узлов | Нет (только 1 байт: `[0x04]`) |
| `0x05` | `CMD_REQ_FULL_SYNC` | Запросить выгрузку всей топологии | Нет (только 1 байт: `[0x05]`) |
| `0x06` | `CMD_REQ_IDENTITY` | Запросить текущие Имя и Роль | Нет (только 1 байт: `[0x06]`) |
| `0x07` | `CMD_REQ_SYS_CONFIG` | Запросить текущие таймеры | Нет (только 1 байт: `[0x07]`) |

### Уведомления из TX (Донгл ➔ Смартфон)
| OpCode | Константа | Описание | Формат Payload |
|---|---|---|---|
| `0x10` | `EVT_MY_STATUS` | Локальная телеметрия Донгла | Структура `BleEvtMyStatus` |
| `0x11` | `EVT_NODE_UPDATE` | Данные о соседнем узле | Структура `BleEvtNodeUpdate` |
| `0x12` | `EVT_IDENTITY` | Ответ с Именем и Ролью (на `0x06`) | Структура `BleIdentity` |
| `0x13` | `EVT_SYS_CONFIG` | Ответ с таймерами (на `0x07`) | Структура `BleSysConfig` |

---

## 4. Структуры данных (Payloads)

### 4.1. Идентификация (`BleIdentity`)
**Используется для:** `CMD_SET_IDENTITY` (0x01) и `EVT_IDENTITY` (0x12)
**Общий размер:** 15 байт

| Смещение | Тип | Размер | Поле | Описание |
| :---: | --- | :---: | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x01` или `0x12` |
| 1 | `uint8` | 1 | `myNodeId` | Локальный ID Донгла (1-255) |
| 2 | `char[]` | 12 | `myName` | Имя узла (UTF-8, Null-terminated) |
| 14 | `uint8` | 1 | `myRole` | Роль узла (0 = Relay, 1 = Stalker, 2 = Tracker) |

---

### 4.2. Системные Настройки (`BleSysConfig`)
**Используется для:** `CMD_SET_SYS_CONFIG` (0x02) и `EVT_SYS_CONFIG` (0x13)
**Общий размер:** 17 байт

| Смещение | Тип | Размер | Поле | Описание |
| :---: | --- | :---: | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x02` или `0x13` |
| 1 | `uint32` | 4 | `txIntervalMoving` | Таймаут отправки в движении (мс) |
| 5 | `uint32` | 4 | `txIntervalStill` | Таймаут отправки на стоянке (мс) |
| 9 | `uint32` | 4 | `nodeConnectionTimeout` | Время до статуса "Потеря связи" (мс) |
| 13 | `uint32` | 4 | `nodeActiveTimeoutMs` | Время до полного удаления из БД (мс) |

---

### 4.3. Обновление Узла (`BleEvtNodeUpdate`)
**Используется для:** `EVT_NODE_UPDATE` (0x11)
**Общий размер:** 39 байт

| Смещение | Тип | Размер | Поле | Описание |
| :---: | --- | :---: | --- | --- |
| 0 | `uint8` | 1 | `opCode` | Всегда `0x11` |
| 1 | `uint8` | 1 | `nodeId` | ID соседнего узла (1-255) |
| 2 | `uint8` | 1 | `nodeRole` | Роль соседнего узла (0, 1 или 2) |
| 3 | `char[]` | 12 | `nodeName` | Имя соседнего узла |
| 15 | `float` | 4 | `lat` | Широта (Широта GPS) |
| 19 | `float` | 4 | `lon` | Долгота (Долгота GPS) |
| 23 | `float` | 4 | `distance` | Дистанция до узла в метрах |
| 27 | `float` | 4 | `azimuth` | Азимут на узел в градусах (0-360) |
| 31 | `float` | 4 | `snr` | Уровень сигнала (SNR) в dB |
| 35 | `uint32` | 4 | `lastSeenAge`| Мс назад, когда узел выходил на связь |

---

### 4.4. Статус Донгла (`BleEvtMyStatus`)
**Используется для:** `EVT_MY_STATUS` (0x10)
**Общий размер:** 5 байт

| Смещение | Тип | Размер | Поле | Описание |
| :---: | --- | :---: | --- | --- |
| 0 | `uint8` | 1 | `opCode` | Всегда `0x10` |
| 1 | `uint8` | 1 | `gpsValid` | 1 - координаты зафиксированы, 0 - поиск спутников |
| 2 | `uint8` | 1 | `satellites` | Количество видимых спутников |
| 3 | `uint8` | 1 | `batteryPercent` | Заряд батареи (0-100%) |
| 4 | `uint8` | 1 | `txQueueSize`| Количество пакетов LoRa в очереди на отправку |