Вот полный обновленный файл контракта, учитывающий изменения до версии 1.47.1 (трехпозиционный статус GPS и сохранение опоры в NVS для слепых ретрансляторов).

# Спецификация BLE Протокола Naviga

**Версия:** 1.47.1

**Устройства:** Naviga-Dongle (C++) ⟷ Naviga-Operator (Flutter/Dart)

**Статус:** Действующий (Трехпозиционный статус GPS, NVS-память для опорной точки и темперирование потока)

## 1. Базовые правила (Общие для всех пакетов)

* **Порядок байтов (Endianness):** Little Endian.
* **Выравнивание памяти (Padding):** Строго 1 байт (отсутствует). В C++ используется `#pragma pack(push, 1)`. Приложение должно читать байты строго подряд без пропусков.
* **Строки (char[]):** Фиксированный размер массива. Строка завершается нуль-терминатором (`\0`, `0x00`). При парсинге необходимо обрезать строку по первому встречному нулевому байту.
* **Размер MTU:** Максимальный размер пакета составляет 43 байта. Рекомендуется запросить MTU не менее 64 байт при подключении.
* **Темперирование (Rate-Limiting):** При массовой выгрузке данных (например, Full Sync) Донгл отправляет пакеты асинхронно с гарантированной задержкой не менее **20 миллисекунд** между трансляциями для предотвращения переполнения буфера NimBLE.

---

## 2. Профиль GATT (Идентификаторы BLE)

* **Имя устройства (Device Name):** Динамическое, формат `Naviga-XXXX` (где XXXX — последние 4 символа MAC-адреса Bluetooth).
* **Service UUID:** `6E400001-B5A3-F393-E0A9-E50E24DCCA9E`
* **RX Characteristic UUID:** `6E400002-B5A3-F393-E0A9-E50E24DCCA9E` (Смартфон ➔ Донгл)
* **TX Characteristic UUID:** `6E400003-B5A3-F393-E0A9-E50E24DCCA9E` (Донгл ➔ Смартфон)

---

## 3. Таблица Команд (OpCodes)

Первый байт ЛЮБОГО пакета всегда является кодом операции (`OpCode`).

### Команды в RX (Смартфон ➔ Донгл)

| OpCode | Константа | Описание | Payload |
| --- | --- | --- | --- |
| `0x01` | `CMD_SET_IDENTITY` | Задать Имя и Роль Донгла | `BleIdentity` |
| `0x02` | `CMD_SET_SYS_CONFIG` | Задать таймеры системы | `BleSysConfig` |
| `0x03` | `CMD_ACTION_RESET` | Аппаратная перезагрузка | Нет |
| `0x04` | `CMD_ACTION_CLEAR_DB` | Очистить базу узлов | Нет |
| `0x05` | `CMD_REQ_FULL_SYNC` | Запросить выгрузку всей топологии | Нет |
| `0x06` | `CMD_REQ_IDENTITY` | Запросить текущие Имя и Роль | Нет |
| `0x07` | `CMD_REQ_SYS_CONFIG` | Запросить текущие таймеры | Нет |
| `0x08` | `CMD_SET_ANCHOR_COORDS` | Передать координаты телефона как опорные | `BleCmdAnchorCoords` |

### Уведомления из TX (Донгл ➔ Смартфон)

| OpCode | Константа | Описание | Формат Payload |
| --- | --- | --- | --- |
| `0x10` | `EVT_MY_STATUS` | Локальная телеметрия Донгла | `BleEvtMyStatus` |
| `0x11` | `EVT_NODE_UPDATE` | **Full Sync Only:** Полные данные об узле (включая себя) | `BleEvtNodeUpdate` |
| `0x12` | `EVT_IDENTITY` | Ответ с Именем и Ролью | `BleIdentity` |
| `0x13` | `EVT_SYS_CONFIG` | Ответ с таймерами | `BleSysConfig` |
| `0x14` | `EVT_NODE_DELETE` | Уведомление об удалении узла | `BleEvtNodeDelete` |
| `0x15` | `EVT_NODE_COORDS` | **Delta:** Обновление координат | `BleEvtNodeCoords` |
| `0x16` | `EVT_NODE_INFO` | **Delta:** Обновление Имени и Роли | `BleEvtNodeInfo` |

---

## 4. Структуры данных (Payloads)

### 4.1. Идентификация (`BleIdentity`)

**Размер:** 27 байт. Поле `myNodeId` игнорируется Донглом при записи (управляется аппаратно), но возвращается для чтения.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x01` / `0x12` |
| 1 | `uint8` | 1 | `myNodeId` | Локальный ID Донгла |
| 2 | `char[]` | 24 | `myName` | Имя (Null-terminated) |
| 26 | `uint8` | 1 | `myRole` | 0=Relay, 1=Stalker, 2=Tracker |

### 4.2. Системные Настройки (`BleSysConfig`)

**Размер:** 17 байт.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x02` / `0x13` |
| 1 | `uint32` | 4 | `txMoving` | Интервал в движении (мс) |
| 5 | `uint32` | 4 | `txStill` | Интервал стоянки (мс) |
| 9 | `uint32` | 4 | `connTimeout` | Потеря связи (мс) |
| 13 | `uint32` | 4 | `activeTimeout` | Удаление из БД (мс) |

### 4.3. Обновление Узла — Full Sync (`BleEvtNodeUpdate`)

**Размер:** 43 байта. Используется **только** при полной синхронизации базы (OpCode 0x05). Пакет высылается для всех активных записей, включая собственный ID Донгла.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x11` |
| 1 | `uint8` | 1 | `nodeId` | ID узла |
| 2 | `uint8` | 1 | `nodeRole` | Роль узла (0=Relay, 1=Stalker, 2=Tracker) |
| 3 | `char[]` | 24 | `nodeName` | Имя (Null-terminated) |
| 27 | `float` | 4 | `lat` | Широта |
| 31 | `float` | 4 | `lon` | Долгота |
| 35 | `float` | 4 | `snr` | SNR (dB) |
| 39 | `uint32` | 4 | `lastSeenAge` | Мс назад (только в этом пакете) |

### 4.4. Статус Донгла (`BleEvtMyStatus`)

**Размер:** 6 байт. Поле `gpsState` отражает аппаратный статус GPS-модуля.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x10` |
| 1 | `uint8` | 1 | `gpsState` | 0 = Поиск (Нет фикса), 1 = Fix OK, 2 = Нет оборудования (Слепое Реле) |
| 2 | `uint8` | 1 | `satellites` | Кол-во видимых спутников |
| 3 | `uint8` | 1 | `batteryPct` | Заряд (0-100%) |
| 4 | `uint16` | 2 | `batteryV` | Напряжение на аккумуляторе (мВ) |

### 4.5. Удаление Узла (`BleEvtNodeDelete`)

**Размер:** 2 байта.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x14` |
| 1 | `uint8` | 1 | `nodeId` | ID удаляемого узла |

### 4.6. Delta: Обновление координат (`BleEvtNodeCoords`)

**Размер:** 14 байт. Основной пакет для передачи перемещений в реальном времени.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x15` |
| 1 | `uint8` | 1 | `nodeId` | ID узла |
| 2 | `float` | 4 | `lat` | Широта |
| 6 | `float` | 4 | `lon` | Долгота |
| 10 | `float` | 4 | `snr` | SNR (dB) |

### 4.7. Delta: Обновление Имени и Роли (`BleEvtNodeInfo`)

**Размер:** 27 байт. Отправляется при получении NodeInfo из радиоэфира.

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x16` |
| 1 | `uint8` | 1 | `nodeId` | ID узла |
| 2 | `uint8` | 1 | `nodeRole` | Роль узла (0=Relay, 1=Stalker, 2=Tracker) |
| 3 | `char[]` | 24 | `nodeName` | Имя (Null-terminated) |

### 4.8. Инициализация опорной точки (`BleCmdAnchorCoords`)

**Размер:** 9 байт. Отправляется смартфоном для предоставления математической базы распаковки. Для мобильных узлов, временно потерявших спутники (`gpsState == 0`), сохраняется только в RAM. Для узлов без аппаратного GPS (`gpsState == 2`), точка перманентно записывается во флеш-память (NVS).

| Смещение | Тип | Размер | Поле | Описание |
| --- | --- | --- | --- | --- |
| 0 | `uint8` | 1 | `opCode` | `0x08` |
| 1 | `float` | 4 | `lat` | Текущая широта смартфона |
| 5 | `float` | 4 | `lon` | Текущая долгота смартфона |